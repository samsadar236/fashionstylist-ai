# Core ML / embeddings
torch>=2.1
transformers>=4.40
pillow>=10.0

# Vector store
chromadb>=0.5

# Data
pandas>=2.0
numpy>=1.24

# LLM
google-generativeai>=0.7

# UI
streamlit>=1.36

# Optional: learned pairwise compatibility scorer (src/compat_model.py)
scikit-learn>=1.3

# NOTE (Windows, no NVIDIA GPU): if `pip install torch` pulls a CUDA build you
# don't want, install the CPU wheel instead:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
